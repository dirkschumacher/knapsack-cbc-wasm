# knapsack-cbc-wasm

WIP - mostly notes for development

## setup

MacOS

```
brew tap coin-or-tools/coinor
brew install cbc
```

Install emscripten

## compile

* In the emscripten directory bash
     * source ./emsdk_env.sh
     * cd <this directory>
     * make